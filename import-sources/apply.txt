# The authorisation for a single import write. ARMED.
#
# This file is read by `scripts/catalogue-import-runner.mjs`. The first line
# that is not blank and not a comment is a token; it is claimed in the
# `console_runs` table before a single row is written, with the same
# `INSERT ... ON CONFLICT DO NOTHING RETURNING id` the D1 console uses. So it
# authorises exactly one run and then spends itself — a later push finds the
# token already recorded and stays a dry run.
#
# The token at the bottom is live: the owner authorised this run in as many
# words — «نفذ وانشر». It writes once and then spends itself.
#
# ## What the dry run said this would do, and what was authorised
#
# 2026-09-16T17:06Z, against a catalogue of 1,275 products, in `refresh-content`:
#
#   create    439    the games the interrupted browser run never reached
#   update    1,085  games already on sale — title, cover, store link, NSUID,
#                    publisher, languages, and provably nothing else: not price,
#                    cost, stock, visibility, options, types, trade-in value or
#                    display order
#   skip      6
#   covers    59 of the 607 covered rows have their cover dropped, because those
#             25 URLs each sit on more than one *different* game. Every link in
#             the file resolves; the artwork on those rows simply belongs to
#             another game — `NipponMarathon` on three Railway Nippon! titles,
#             a Wii `deBlob` packshot on Densha de GO!!, a Just Dance portrait
#             on Dance with Devils. Pass `--covers all` to import them anyway.
#
# Result if applied: 1,714 products, 548 of them with verified artwork.

2026-09-16-catalogue-content-refresh
